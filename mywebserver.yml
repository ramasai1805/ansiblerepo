---
- hosts: all
  become: yes
  tasks:
    - name: install tree
      apt:
        name: tree
        state: present
    - name: create a file
      file:
        path: /home/ansible/mytestfile
        state: touch
